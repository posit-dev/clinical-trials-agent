# lab graph arguments are validated

    Code
      measure_env$lab_by_arm_plot(parameter = "BAD")
    Condition
      Error in `measure_env$lab_by_arm_plot()`:
      ! `parameter` must be one of "ALT", "CRP", or "IGA".

